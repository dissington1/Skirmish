function setHidden(button, condition) {
    button.hidden = !!condition;
}

function setDisabled(button, condition) {
    button.disabled = !!condition;
}

function toggleFolder(folder, condition) {
    folder.hidden = !!condition;
}